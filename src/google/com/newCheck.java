package google.com;

public class newCheck extends Main{
    StringBuilder token = new StringBuilder();

    newCheck(String s) {
        int index_first = 0, index_current = 0;
        while (index_first < s.length()) {

        }
    }
}
